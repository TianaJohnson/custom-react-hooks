import { useState, useEffect } from 'react';

let cached = [];

const useScript = (src) => {

    const [ status, setStatus ] = useState({

        loaded: false,
        error: false
    })

    useEffect (() => {
        effect

        return () => {
            cleanup
        }
    })



}